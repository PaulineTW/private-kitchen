<div class="dashboard_banner">
  <div class="card-category card-large" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('dashboard_banner.png') %>)">
    <h1>My Dashboard</h1>
  </div>
</div>

<%# <div class="container m-3">
  <ul class="nav nav-tabs" id="myTab" role="tablist">
    <li class="nav-item" role="presentation">
      <button class="nav-link active" id="mybookings-tab" data-bs-toggle="tab" data-bs-target="#mybookings" type="button" role="tab" aria-controls="mybookings" aria-selected="true">My bookings</button>
    </li>
    <li class="nav-item" role="presentation">
      <button class="nav-link" id="mykitchens-tab" data-bs-toggle="tab" data-bs-target="#mykitchens" type="button" role="tab" aria-controls="mykitchens" aria-selected="false">My kitchens</button>
    </li>
  </ul>
  <div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="mybookings" role="tabpanel" aria-labelledby="mybookings-tab"> bookings </div>
    <div class="tab-pane fade" id="mykitchens" role="tabpanel" aria-labelledby="mykitchens-tab"> my kitchens </div>
  </div>
</div> %>

<div class="tabs-container">
  <ul class="tabs">
    <li class="active">
      <a href="">My Bookings</a>
    </li>
    <li>
      <a href="">My Kitchens</a>
    </li>
  </ul>
  <div class="tabs-content">
    <div class="tabs-panel active" data-index="0">
      <!-- content here -->
      <div class= "container">
 <div class= "card">
    <% @kitchen.bookings.each do |booking| %>
      <% booking = Booking.find_by(kitchen: @kitchen) %>
      <div class="card m-3" style="width: 18rem;">
        <%= image_tag "indian_food.jpg" %>
          <div class="card-body">
            <h5 class="card-title"><%= kitchen.title%></h5>
            <p class="card-text"><%= kitchen.cuisine%></p>
            <p class="date"><%= booking.date%></p>
            <p class="user_name"><%= user.last_name%></p>
          </div>
        </div>
      </div>
    <% end %>
 </div>
</div>

    </div>
    <div class="tabs-panel" data-index="1">
      <!-- content here -->
      <h1>My bookings</h1>
    </div>
  </div>
</div>

<%= Booking.all %>
